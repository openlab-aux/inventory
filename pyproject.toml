[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "hackinv"
dynamic = ["version"]

[tool.setuptools.dynamic]
version = {attr = "hackinv.VERSION"}

